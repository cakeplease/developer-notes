A Nais job is used for tasks meant to complete and then exit. This can either run as a one-off task or on a schedule like a cron job. A job is defined by job manifest (YAML file), that describes how the job should be run and what resources it needs.

https://docs.nais.io/workloads/job/ 