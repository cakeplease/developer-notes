Performing and exploring actions on cluster.
While you are running it, it will pick up the context that is currently active in kube config file.
**shift + A** - sort on age of pods
Can use **right arrow** to see all columns with age of the pods.
Shift + S - sort by status

`L` - get logs
/searchPhrase - filter/search

eller 
`k logs pods` etc

press **s** to go into shell for chosen pod, **ctrl + D** to exit
**Y** to see yaml